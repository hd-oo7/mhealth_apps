.DS_Store
.ipynb_checkpoints/
__pycache__/
*.pyc

# generated output -- not part of the release
mscan/results/
figures/

# data/ holds both the raw corpus (real app_id, not released -- see Ethics
# appendix's no-re-identification commitment) and the two anonymized files
# that ship with this release. Ignore everything, then allow just those two.
data/*
!data/anonymized_app_list.csv
!data/mhealth_apps_metrics_anonymized.csv

.env
